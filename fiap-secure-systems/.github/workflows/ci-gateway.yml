name: CI – gateway-service

on:
  pull_request:
    paths:
      - "fiap-secure-systems/gateway-service/**"
      - "fiap-secure-systems/build.gradle.kts"
      - "fiap-secure-systems/settings.gradle.kts"
      - "fiap-secure-systems/gradle/**"
      - "fiap-secure-systems/gradlew"
      - "fiap-secure-systems/infrastructure/contracts/**"
      - ".github/workflows/ci-gateway.yml"
  push:
    branches:
      - main
    paths:
      - "fiap-secure-systems/gateway-service/**"
      - "fiap-secure-systems/build.gradle.kts"
      - "fiap-secure-systems/settings.gradle.kts"
      - "fiap-secure-systems/gradle/**"
      - "fiap-secure-systems/gradlew"
      - "fiap-secure-systems/infrastructure/contracts/**"
      - ".github/workflows/ci-gateway.yml"

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/fiap-gateway-service

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: fiap-secure-systems

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('fiap-secure-systems/**/*.gradle.kts', 'fiap-secure-systems/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Unit tests
        run: ./gradlew :gateway-service:test --console=plain

      - name: Integration tests
        run: ./gradlew :gateway-service:integrationTest --console=plain

      - name: Build JAR
        run: ./gradlew :gateway-service:bootJar --console=plain

      - name: Set up Docker Buildx
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: fiap-secure-systems
          file: fiap-secure-systems/gateway-service/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.sha }}
            ${{ env.IMAGE }}:main
          cache-from: type=gha,scope=gateway
          cache-to: type=gha,mode=max,scope=gateway
